# =============================================================================
# Maestro E2E Test Configuration — DEV BUILD
# =============================================================================
# appId: com.toroloom.app (development build)
# =============================================================================

appId: com.toroloom.app
flows:
  - "flows/**/*.yaml"
env:
  TEST_EMAIL: test@toroloom.com
  TEST_PASSWORD: password123
