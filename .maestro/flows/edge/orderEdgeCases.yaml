# =============================================================================
# Order Edge Cases Flow
# =============================================================================
# Tests the order form edge cases:
#   1. Login with test credentials
#   2. Markets → search RELIANCE → Stock Detail
#   3. Buy → "Buy Securities" form
#   4. Max quick-qty with empty balance → error / clamp behaviour
#   5. Order Summary, order types, balance info
#   6. Back to stock detail
# =============================================================================

appId: com.toroloom.app
---
- clearState
- launchApp
- waitForAnimationToEnd:
    timeout: 15000
# Cold-start can be slow on the cloud emulator — wait up to 60s
- extendedWaitUntil:
    visible:
      id: "login-welcome-back"
    timeout: 60000
- tapOn:
    id: "login-email-input"
- inputText: ${TEST_EMAIL}
- hideKeyboard
- tapOn:
    id: "login-password-input"
- inputText: ${TEST_PASSWORD}
- hideKeyboard
- tapOn:
    id: "login-btn"
- waitForAnimationToEnd:
    timeout: 30000

# Fresh installs show the onboarding walkthrough after login — skip it
# (idempotent: skipped when already completed)
- runFlow:
    when:
      visible:
        text: "Skip"
    commands:
      - tapOn:
          text: "Skip"
      - waitForAnimationToEnd:
          timeout: 5000

# If the backend health check fails (no local backend), dismiss the
# offline modal so the Home screen is reachable (idempotent).
- runFlow:
    when:
      visible:
        text: "You're offline"
    commands:
      - tapOn:
          text: "Close"
      - waitForAnimationToEnd:
          timeout: 5000
- assertVisible:
    id: "home-greeting"

# ═══════════════════════════════════════════════════════════════════════════
# 1. Navigate to Markets → Stock Detail
# ═══════════════════════════════════════════════════════════════════════════
- tapOn:
    id: "tab-markets"
- waitForAnimationToEnd
- assertVisible:
    id: "markets-title"

# RELIANCE chip lives in the suggestion dashboard, visible ONLY while the
# search bar is focused AND the query is EMPTY — typing hides the chips and
# shows API results instead. Tap the chip right after focusing.
- tapOn:
    text: "Search stocks by name or symbol..."
- waitForAnimationToEnd:
    timeout: 3000
- extendedWaitUntil:
    visible:
      text: "RELIANCE"
    timeout: 20000
- tapOn:
    text: "RELIANCE"
- waitForAnimationToEnd
- assertVisible:
    text: "RELIANCE"

# ═══════════════════════════════════════════════════════════════════════════
# 2. Open Buy Order Form
# ═══════════════════════════════════════════════════════════════════════════
# Bottom action bar Buy button (testID stock-buy-btn)
- tapOn:
    id: "stock-buy-btn"
- waitForAnimationToEnd

# Verify the order form screen loaded
- assertVisible:
    text: "Buy Securities"

# ═══════════════════════════════════════════════════════════════════════════
# 3. Test Max Quantity Button
# ═══════════════════════════════════════════════════════════════════════════
# Quick quantity chips: 10 / 50 / 100 / Max (testID quickQty_Max)
- tapOn:
    id: "quickQty_Max"
- waitForAnimationToEnd

# The Order Summary card appears when qty is set — scroll to it
- scrollUntilVisible:
    element:
      text: "Order Summary"
    direction: DOWN
- assertVisible:
    text: "Order Summary"

# Verify the balance indicator is visible
- assertVisible:
    text: "Available"

# ═══════════════════════════════════════════════════════════════════════════
# 4. Verify Order Form Shows Order Types
# ═══════════════════════════════════════════════════════════════════════════
# Market order type chip (orderType_market)
- assertVisible:
    text: "Market"

# Tap the back button (testID) to return to stock detail
- tapOn:
    id: "backBtn"
- waitForAnimationToEnd
- assertVisible:
    text: "RELIANCE"
