# =============================================================================
# PlaceOrder Screen — End-to-End Test
# =============================================================================
# Tests the full order placement flow:
#   1. Login and navigate to Markets → Stock Detail
#   2. Open Buy order form from stock detail
#   3. Verify form layout: header, stock info, toggles, selectors
#   4. Toggle between Buy and Sell
#   5. Change order type (MARKET → LIMIT → SL → SL-M → back to MARKET)
#   6. Change product type (CNC → MIS)
#   7. Set quantity via quick preset buttons
#   8. Verify Order Summary card appears with cost breakdown
#   9. Place the order
#   10. Verify confirmation modal appears with order details
#   11. Tap Done and return to stock detail
# =============================================================================
# Uses testID-based targeting where available for robustness.
# Coordinate taps are only used as last resort for elements without
# text labels or testIDs (e.g., +/- quantity adjust buttons).
# =============================================================================

appId: com.toroloom.app
---
- clearState
- launchApp
- waitForAnimationToEnd:
    timeout: 15000
# ── 0. Kill app → relaunch fresh (prevents state leakage between sequential flows) ──

# ── 1. Login ──────────────────────────────────────────────────────────────
- assertVisible:
    id: "login-email-input"
- tapOn:
    id: "login-email-input"
- inputText: ${TEST_EMAIL}
- hideKeyboard
- tapOn:
    id: "login-password-input"
- inputText: ${TEST_PASSWORD}
- hideKeyboard
- tapOn:
    id: "login-btn"
- waitForAnimationToEnd:
    timeout: 5000

# Fresh installs show the onboarding walkthrough after login — skip it
# (idempotent: skipped when already completed)
- runFlow:
    when:
      visible:
        text: "Skip"
    commands:
      - tapOn:
          text: "Skip"
      - waitForAnimationToEnd:
          timeout: 5000

# If the backend health check fails (no local backend), dismiss the
# offline modal so the Home screen is reachable (idempotent).
- runFlow:
    when:
      visible:
        text: "You're offline"
    commands:
      - tapOn:
          text: "Close"
      - waitForAnimationToEnd:
          timeout: 5000

# ═══════════════════════════════════════════════════════════════════════════
# 2. Navigate: Markets → Stock Detail → Buy
# ═══════════════════════════════════════════════════════════════════════════
- tapOn:
    id: "tab-markets"
- waitForAnimationToEnd
- assertVisible:
    id: "markets-title"

# Tap RELIANCE (always listed, but may be below the fold — scroll it in)
- scrollUntilVisible:
    element:
      text: "RELIANCE"
    direction: DOWN
    speed: 20
    timeout: 30000
- tapOn:
    text: "RELIANCE"
- waitForAnimationToEnd
- assertVisible:
    text: "RELIANCE"

# Tap Buy button in bottom action bar to open PlaceOrderScreen
- tapOn:
    id: "stock-buy-btn"
- waitForAnimationToEnd

# ═══════════════════════════════════════════════════════════════════════════
# 3. Verify PlaceOrder Form — Initial Layout (Buy mode, MARKET, CNC)
# ═══════════════════════════════════════════════════════════════════════════
- assertVisible:
    id: "headerTitle"

# Verify stock info is displayed in the header
- assertVisible:
    text: "RELIANCE"

# Verify buy toggle is active (testID targeting — i18n-safe)
- assertVisible:
    id: "buyToggle"
- assertVisible:
    id: "sellToggle"

# Verify default selections are visible (chip enum values are hardcoded ✅)
- assertVisible:
    id: "orderType_MARKET"
- assertVisible:
    id: "productType_CNC"

# Verify quick quantity presets are visible
- assertVisible:
    text: "10"
- assertVisible:
    text: "50"
- assertVisible:
    text: "100"
- assertVisible:
    text: "Max"

# ═══════════════════════════════════════════════════════════════════════════
# 4. Toggle to Sell Mode, Then Back to Buy
# ═══════════════════════════════════════════════════════════════════════════
- tapOn:
    id: "sellToggle"
- waitForAnimationToEnd

# Header should show "Sell Securities" via testID
- assertVisible:
    id: "headerTitle"

# Toggle back to Buy for the order placement flow
- tapOn:
    id: "buyToggle"
- waitForAnimationToEnd
- assertVisible:
    id: "headerTitle"

# ═══════════════════════════════════════════════════════════════════════════
# 5. Cycle Through Order Types
# ═══════════════════════════════════════════════════════════════════════════
# Default is MARKET — tap LIMIT
- tapOn:
    id: "orderType_LIMIT"
- waitForAnimationToEnd

# Limit price input should appear
- assertVisible:
    text: "LIMIT PRICE (₹)"

# Tap SL (Stop Loss) — adds trigger price field
- tapOn:
    id: "orderType_SL"
- waitForAnimationToEnd

# Trigger price input should appear alongside limit price
- assertVisible:
    text: "TRIGGER PRICE (₹)"

# Tap SL-M (Stop Loss Market) — both price fields stay visible
- tapOn:
    id: "orderType_SL-M"
- waitForAnimationToEnd

- assertVisible:
    text: "LIMIT PRICE (₹)"
- assertVisible:
    text: "TRIGGER PRICE (₹)"

# Tap back to MARKET for the actual order placement
- tapOn:
    id: "orderType_MARKET"
- waitForAnimationToEnd

# ═══════════════════════════════════════════════════════════════════════════
# 6. Change Product Type to MIS (Intraday)
# ═══════════════════════════════════════════════════════════════════════════
- tapOn:
    id: "productType_MIS"
- waitForAnimationToEnd

# Verify the description changed to mention intraday (actual label is
# "Intraday — square off by EOD" — use a regex prefix match)
- assertVisible:
    text: "Intraday.*"

# ═══════════════════════════════════════════════════════════════════════════
# 7. Set Quantity Using Quick Preset
# ═══════════════════════════════════════════════════════════════════════════
# Use the "50" quick quantity button
- tapOn:
    id: "quickQty_50"
- waitForAnimationToEnd

# ═══════════════════════════════════════════════════════════════════════════
# 8. Verify Order Summary Card
# ═══════════════════════════════════════════════════════════════════════════
- scrollUntilVisible:
    element:
      text: "Order Summary"
    direction: DOWN
    speed: 20
    timeout: 30000
# Let the summary settle after the qty change (values recompute → re-render)
- waitForAnimationToEnd:
    timeout: 5000
# Verify Order Summary section via testIDs — i18n-safe. The card is tall —
# scroll the BOTTOM row (Estimated Total) into view so the clipped rows
# (inverted/negative bounds when below the fold) are fully visible.
- scrollUntilVisible:
    element:
      text: "Estimated Total"
    direction: DOWN
    speed: 20
    timeout: 30000
- assertVisible:
    id: "order-summary-title"
- assertVisible:
    id: "order-summary-qty"
- extendedWaitUntil:
    visible:
      id: "order-summary-price-per-share"
    timeout: 10000
- assertVisible:
    id: "order-summary-est-total"

# ═══════════════════════════════════════════════════════════════════════════
# 9. Place the Order
# ═══════════════════════════════════════════════════════════════════════════
# Scroll down to the bottom action bar to find the place order button
- scrollUntilVisible:
    element:
      text: "Buy 50 RELIANCE"
    direction: DOWN

# The bottom bar (Buy 50 RELIANCE) is fixed-position and always visible
- assertVisible:
    text: "Buy 50 RELIANCE"

# Tap the place order button
- tapOn:
    text: "Buy 50 RELIANCE"

# ═══════════════════════════════════════════════════════════════════════════
# 10. Verify Confirmation Modal
# ═══════════════════════════════════════════════════════════════════════════
# Wait for the 1.5s simulated network delay + modal animation
- waitForAnimationToEnd:
    timeout: 8000

# Verify confirmation modal is visible
- assertVisible:
    id: "confirmationModal"

# Verify confirmation details text (dynamic order data — safe)
- assertVisible:
    text: "Bought 50 shares of RELIANCE"
- assertVisible:
    text: "Market · MIS"

# ═══════════════════════════════════════════════════════════════════════════
# 11. Dismiss Confirmation → Return to Stock Detail
# ═══════════════════════════════════════════════════════════════════════════
- tapOn:
    id: "doneBtn"
- waitForAnimationToEnd

# Should navigate back to StockDetail screen
- assertVisible:
    text: "RELIANCE"
