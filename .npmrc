# Pin the npm registry for ALL installs in this repo.
# Without this, a contributor's machine configured with a mirror (e.g.
# registry.npmmirror.com) bakes mirror URLs into package-lock.json —
# supply-chain integrity then depends on that mirror. Enforced further by
# scripts/verify-lockfile.mjs (CI gate).
registry=https://registry.npmjs.org/
